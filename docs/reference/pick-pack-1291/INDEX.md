# Pick Pack 1291 Reference Index

Status: READ-ONLY REFERENCE INDEX

## Source archive

Primary archive location: Google Drive project root `VẬN HÀNH DC HƯNG YÊN` → folder `BACKUP PICK PACK 1291`.

Existing canonical digest: `../PICK_PACK_1291_REFERENCE.md`.

## Rule

Pick Pack 1291 is evidence, not authority.

No legacy behavior becomes a VHDCHY requirement until evaluated and recorded as:

- `ADOPTED` — reused substantially as-is because it fits current VHDCHY scope;
- `ADAPTED` — pattern reused with explicit VHDCHY differences;
- `REJECTED` — reviewed but intentionally not used.

Anything merely observed remains `REFERENCE`/`EVALUATED`, not implementation authority.

## When to read the full backup

Only when a task needs provenance or implementation detail not present in the digest, especially for cluster Pick Pack 1291: data model, workflow/UX, offline sync, event/idempotency, Sheets projection, LAN/DR/fallback, release lessons.

Do not load the full archive by default in every chat/session.

## Safety

Do not copy secrets, signing credentials, private keys or retired runtime endpoints from backup into VHDCHY source/docs. Do not write to the retired project.
