# NEXT ACTIONS

Baseline: `REPO-RESET-20260912-01`

## Immediate priority — Service/BETA

1. Verify Google Cloud/OAuth under `tam95.supra@gmail.com`.
2. Verify Cloudflare account/zone/expected BETA Worker+D1 under `nguyenvantam050595@gmail.com`.
3. Verify VHDCHY BETA Android signer locally.
4. After Google Cloud/OAuth is ready, create/link/deploy GAS BETA using the retained Google Gateway foundation.
5. Rebuild GitHub `beta` Environment only from verified provider outputs.
6. Recreate Worker/D1 source and deployment workflow only after Cloudflare resource identity is verified and the schema-version contract is reconciled.
7. Run BETA CI/deploy/health gate.

## LAN

No physical LAN work while Owner is off-site. When Owner returns to the company, resume the pending V4 physical regression and run independent LAN/Service items in parallel.

## STABLE

Blocked until BETA PASS and explicit Owner approval. Do not copy BETA credentials/IDs into STABLE.

## Manual GitHub cleanup

After the clean baseline is verified, remove obsolete branches/tags through GitHub UI as listed by the assistant. Do not remove `main`; keep the pre-zero backup branch until cleanup is explicitly performed.
