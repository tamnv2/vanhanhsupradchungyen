name: VHDCHY dormant guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dormant:
    runs-on: ubuntu-latest
    steps:
      - name: Dormant project guard
        run: |
          echo 'VHDCHY is DORMANT_RESET_ZERO.'
          echo 'No automatic CI/provider activity is authorized.'
          echo 'Review AI_ENTRYPOINT.md, RESET_STATE.md and RESOURCE_MANIFEST.md before any future restart.'
